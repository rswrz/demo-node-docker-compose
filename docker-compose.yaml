version: '3'
services:

  app:
    build: .
    command: npm run dev
    volumes:
      - ./app:/app
      - /app/node_modules
    ports:
    - "3000:3000"
    depends_on:
      - mongo

  mongo:
    image: mongo
